document.addEventListener("DOMContentLoaded", function () { // reCAPTCHA token assignment if (typeof grecaptcha !== "undefined") { grecaptcha.enterprise.ready(async () => { const token = await grecaptcha.enterprise.execute('6LfFKQMrAAAAADo1jSQ8JKTG25CUz2eQpAHyiaPn', { action: 'contact_form' }); const tokenInput = document.getElementById("recaptcha_token"); if (tokenInput) { tokenInput.value = token; } }); } // Form validation with improved email checking document.getElementById("contactForm").addEventListener("submit", function (e) { const name = document.getElementById("name").value.trim(); const email = document.getElementById("email").value.trim(); const message = document.getElementById("message").value.trim(); if (!name || !email || !message) { alert("Please fill in all required fields."); e.preventDefault(); return; } // Improved email validation using regex const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; if (!emailRegex.test(email)) { alert("Please enter a valid email address."); e.preventDefault(); return; } });});